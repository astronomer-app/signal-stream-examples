{
  "name": "@astronomer-app/example-alpaca-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Signal Stream example: a Node server that places Alpaca paper options orders on each signal.",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "tsx --env-file=.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@astronomer-app/signals": "^0.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.6"
  }
}
